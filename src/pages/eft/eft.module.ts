import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { EftPage } from './eft';

@NgModule({
  declarations: [
    EftPage,
  ],
  imports: [
    IonicPageModule.forChild(EftPage),
  ],
})
export class EftPageModule {}
